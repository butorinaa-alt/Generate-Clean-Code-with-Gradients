
  # Generate Clean Code with Gradients

  This is a code bundle for Generate Clean Code with Gradients. The original project is available at https://www.figma.com/design/czPS72NerEc58mSxszvOfd/Generate-Clean-Code-with-Gradients.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  